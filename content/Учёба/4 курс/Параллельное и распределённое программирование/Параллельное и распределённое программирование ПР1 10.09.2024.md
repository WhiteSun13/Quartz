1:10 – В чём различие Параллельное и распределённое вычисления
2:30 - 5:30 – НИЧЕГО
9:00 – VS 2022. Bomba211 параллельное программирование в Си шарп
11:20 – Для параллельного программирования нужно подключить директиву using System.Threading.

Объявляем переменную.

long k = 0;
Thread mythread;

Функции потока **всегда** имеют тип Воид.
```C#
Воид НювТхреад(){
	for(long i = 0; i < 949561616465; i++){
		k++;
	}
}
```
В VS встроенна ИнтеллиСенсе

18:20 – Создание потока

19:30 – Определение коструктора. Служить для создания элементов.

Thread mythread = new Thread(newthread); Неправильно! Создавать потоки можно в public Form1.

23:30 – Про область видимости.

```C#
long k = 0;
Thread mythread;

public Form1(){
	mythread = new Thread(newthread);
}
```
```C#
void button_click(amongus){
Label.text = k.ToString
}
```
30:00 – Поток нужно запускать. При создании потока нужно указывать только имя.
```C#
mythread.Start();
```

Повторить, Добавить чтобы ещё и убавлялось (2 потока).